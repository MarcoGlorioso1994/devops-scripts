# python/ 📁

Python utilities for common DevOps checks and analysis.

Contents:

- `api_checker.py` — check configured API endpoints (uses `requests` and `PyYAML`).
- `config_loader.py` — small helper to load YAML configs.
- `log_analyzer.py` — simple log summarizer.

Install deps: `pip install -r requirements.txt`